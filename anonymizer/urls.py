from django.urls import path
from django.conf.urls import include, url
from . import views

urlpatterns = [
	#path(r'^media/', views.view_file, name='view_files'),
	#path('media/user_docs/<path:object_id>/', views.view_file, name = 'view_file'),
	#url(r'^$', views.index, name='index'),
]
